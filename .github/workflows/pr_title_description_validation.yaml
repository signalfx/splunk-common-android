name: PR Title and Description Validation

on:
  pull_request:
    types: [opened, reopened, edited, synchronize]

jobs:
  validate:
    uses: signalfx/splunk-common-android/.github/workflows/pr_title_description_validation_template.yaml
    secrets: inherit
